% file: cdt.tex

\documentclass[tikz]{standalone}

\begin{document}
\begin{tikzpicture}
  \node (obj) [circle, draw, minimum size = 5pt, purple] {};
  \draw[<-, teal] (obj.north east) to +(60:0.6cm);
  \draw[<-, teal] (obj.north west) to +(120:0.6cm);
\end{tikzpicture}
\end{document}