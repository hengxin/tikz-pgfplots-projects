% consistency-fm.tex

\documentclass[tikz]{standalone}
\usetikzlibrary{positioning, fit}

\begin{document}
\begin{tikzpicture}[topic/.style = {draw, rectangle, thick, align = center}]
  % framework
  \node (cm) [blue, topic, dashed] {Specification Framework (for RDT)};

  % consistency models
  \node (dummy) [above = of cm] {};
  \node (ec) [red, topic, left = of dummy] {Weak \\ Consistency \\ (\textsl{Jupiter})};
  \node (sc) [red, topic, right = of dummy] {Strong \\ Consistency \\ (Paxos)};

  % weak consistency
  % \node (wc) [topic, fit = (ec) (cc), label = below : {Weak Consistency}] {};
  % programming
  % \node (prog) [topic, dotted, above = 0.50cm of ec] {Programming \\ over Weak Consistency Models};

  % data consistency problem
  \node (dcp) [topic, dashed, fit = (cm) (ec) (sc), inner sep = 8pt,
    label = above : {Distributed Data Consistency}] {};

  % coq
  \node (coq) [right = 1.60cm of cm, anchor = west] {\includegraphics[scale = 0.50]{figs/coq}};
  % refinement
  \node (refinement) [teal, draw, above = 0.15cm of coq, font = \large] {{\it Refinement}};
  % tla
  \node (tla) [above = 0.15cm of refinement] {\includegraphics[scale = 0.16]{figs/tlaplus}};

  \node (fm) [topic, dashed, inner sep = 0pt, fit = (coq) (tla),
    label = {above : {Formal Methods}}] {};
\end{tikzpicture}
\end{document}