\documentclass{standalone}
\usepackage{pgfplots}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
	  title = ,
	  xlabel = {MPL},
	  ylabel = {Transaction abort rates},
	  xtick = data,
	  ymin = 0,
	  ytick = {0, 0.1, 0.2, 0.3},
	  grid = major,
	  minor y tick num = 1,
	legend entries = {
	    vc$(1,0,0)$ \\
		vc$(2,2,1)$ \\
		vc$(3,3,1)$ \\
		vc$(4,4,1)$ \\
	    wcf$(1,0,0)$ \\
		wcf$(2,2,1)$ \\
		wcf$(3,3,1)$ \\
		wcf$(4,4,1)$ \\
	  },
	  legend pos = outer north east,
	legend style = {
	  legend columns = 4,
	  cells = {anchor = west},
	  font = \footnotesize,
	  transpose legend,
	},
  ]

	\addplot[red, mark = otimes] table[x = mpl, y = vc100] {rw4-aborts.dat};
	\addplot[red, mark = triangle] table[x = mpl, y = vc221] {rw4-aborts.dat};
	\addplot[red, mark = square] table[x = mpl, y = vc331] {rw4-aborts.dat};
	\addplot[red, mark = pentagon] table[x = mpl, y = vc441] {rw4-aborts.dat};

	\addplot[blue, mark = otimes, densely dashed] table[x = mpl, y = wcf100] {rw4-aborts.dat};
	\addplot[blue, mark = triangle, densely dashed] table[x = mpl, y = wcf221] {rw4-aborts.dat};
	\addplot[blue, mark = square, densely dashed] table[x = mpl, y = wcf331] {rw4-aborts.dat};
	\addplot[blue, mark = pentagon, densely dashed] table[x = mpl, y = wcf441] {rw4-aborts.dat};
  \end{axis}
\end{tikzpicture}
\end{document}
