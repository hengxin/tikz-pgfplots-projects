% banking-writeskew.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  node distance = 1.0cm and 2.0cm,
  wr/.style = {->, thick},
  ww/.style = {->, thick, dashed},
  rw/.style = {->, thick, dotted},
  txn/.style = {draw, inner sep = 3pt}]

  \node[txn, label = left : $T_{A}$] (t-alice)
    {$\readevent(C, 50) \;\; \readevent(S, 50) \;\; \writeevent(C, -10)$};
  \node[txn, label = left : $T_{B}$, below = of t-alice] (t-bob)
    {$\readevent(C, 50) \;\; \readevent(S, 50) \;\; \writeevent(S, -10)$};

  \draw[rw, sloped] (t-alice.-90) to node[above, near end]{$\RW(S)$} (t-bob.8);
  \draw[rw, sloped] (t-bob.173) to node[above, near start]{$\RW(C)$} (t-alice.-8);
\end{tikzpicture}
\end{document}