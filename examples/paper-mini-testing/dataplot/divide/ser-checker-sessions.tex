% memory.tex

\documentclass[border={80pt -50pt -50pt 90pt}]{standalone}
% \documentclass{standalone}
\input{newcommands}
\pagestyle{empty} % Remove page numbering
\usepackage[left=0pt, right=0pt, top=0pt, bottom=0pt]{geometry}

\begin{document}

\begin{tikzpicture}[scale=1]
	\begin{axis}[
			ymax=15000,
			xlabel={\#sessions},
			ylabel={Time (ms)},
			cycle multiindex* list={
					color       \nextlist
					mark list*  \nextlist
				},
			legend pos=north west
		]
		\addplot[color=blue,mark=square,mark size=3pt] table [x=sessions, y=cobra, col sep=comma] {../data/ser-checker-sessions.csv};
		\addplot[color=red,mark=triangle,mark size=3pt] table [x=sessions, y=mini, col sep=comma] {../data/ser-checker-sessions.csv};
	\end{axis}
\end{tikzpicture}
\end{document}