% session.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  wr/.style = {->, thick},
  ww/.style = {->, thick, dashed, red},
  rw/.style = {->, thick, dotted, blue},
  txn/.style = {draw, inner sep = 3pt, align = center}]

  \node[txn, label = above : $T_{1}$] (t0)
    {$\readevent(\keyx, 0)$ \po $\writeevent(\keyx, 1)$};

  \node[txn, label = above : $T_{2}$, right = 1.50cm of t0] (t1)
    {$\readevent(\keyx, 1)$ \po $\writeevent(\keyx, 2)$};

  \node[txn, label = above : $T_{3}$, right = 1.50cm of t1] (t2)
    {$\readevent(\keyx, 1)$};

  \draw[->, thick] (t0) to node[above] {$\so$} (t1);
  \draw[->, thick] (t1) to node[above] {$\so$} (t2);
\end{tikzpicture}
\end{document}