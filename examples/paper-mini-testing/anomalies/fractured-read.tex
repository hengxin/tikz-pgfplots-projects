% session.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
    wr/.style = {->, thick},
    ww/.style = {->, thick, dashed, red},
    rw/.style = {->, thick, dotted, blue},
    txn/.style = {draw, inner sep = 3pt, align = center}]

  \node[txn, label = above : $T_{1}$] (t1)
  {$\readevent(\keyxvar, 1)$ \po $\writeevent(\keyxvar, 2)$ \po $\readevent(\keyyvar, 3)$ \po $\writeevent(\keyyvar, 4)$};

  \node[txn, label = above : $T_{2}$, right = 1.50cm of t1] (t2)
  {$\readevent(\keyxvar, 2)$ \po $\readevent(\keyyvar, 0)$};

  \draw[->, thick] (t1) to node[above] {$\WR(\keyxvar)$} (t2);
\end{tikzpicture}
\end{document}