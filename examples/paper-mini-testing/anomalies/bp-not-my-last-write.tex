% bp-not-my-last-write.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, shapes.misc, positioning,
  arrows.meta, decorations.pathmorphing, fit}

\begin{document}
\begin{tikzpicture}
  \tikzset{
    so/.style = {->, thick},
    wr/.style = {->, thick},
    txn/.style = {rectangle, draw, inner sep = 3pt, align = center},
  }

  \node[txn, label = above : $t$] (t)
    {$\readevent(\keyxvar, 0) \hpo \writeevent(\keyxvar, 1) \hpo
      \writeevent(\keyxvar, 2) \hpo \readevent(\keyxvar, 1)$};
\end{tikzpicture}
\end{document}