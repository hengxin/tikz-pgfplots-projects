% ser-ext-item.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  node distance = 1.5cm and 1.0cm,
  wr/.style = {->, draw = green, thick},
  ww/.style = {->, draw = blue, thick},
  rw/.style = {->, draw = teal, thick, dashed},
  txn/.style = {draw, inner sep = 5pt, align = center},
  comp/.style = {->, ultra thick, purple, loosely dash dot}]

  % S
  \node[txn, label = below : {$S \vdash \writeevent(x, v)$}] (s)
    {$S$};
  % T
  \node[txn, label = {[xshift = 15pt] above :
    {$T \vdash \readevent(x, v)$}},
    above right = of s] (t) {$T$};
  % T'
  \node[txn, label = {[xshift = -15pt] above :
    {\(\begin{array}{c}
      T' \vdash \writeevent(x, v') \\
      v' \neq v
      \end{array}\)}},
    above left = of s] (t') {$T'$};

  \draw[->, thick, sloped] (t') to node[above]{$\VIS/\AR$} (t);
  \draw[wr, sloped] (s) to node[above]{$\WR(x)$}
                           node[below]{$\VIS/\AR$} (t.south);
  \draw[ww, sloped] (s) to node[above]{$\WW(x)$}
                           node[below]{$\AR$} (t'.south);

  \draw[rw, bend right = 70, sloped, very thick] (t) to node[below]{$\RW(x)$}
                                    node[above]{$\AR$} (t');
\end{tikzpicture}
\end{document}