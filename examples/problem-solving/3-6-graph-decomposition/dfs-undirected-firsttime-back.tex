% file: 3-6-graph-decomposition/dfs-undirected-firsttime-back.tex

\documentclass[tikz]{standalone}
\usetikzlibrary{positioning}

\begin{document}
\begin{tikzpicture}[vertex/.style = {draw, teal, circle, minimum size = 8pt},
  node distance = 2.0cm and 1.5cm,
  every edge/.style = {draw, thick}]
  \node (u) [vertex] {$u$};
  \node (v) [vertex, below = of u] {$v$};

  \path (v) edge[blue, ->, bend left] node[below, sloped] {$1.$ back edge} (u)
	(u) edge[red, ->, bend left, dashed] node[below, sloped] {$2.$ \textcolor{red}{???}}(v);
\end{tikzpicture}
\end{document}
