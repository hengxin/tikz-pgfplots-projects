% refinement-progress.tex

\documentclass{standalone}

\usepackage{marvosym}
\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\newcommand{\yes}{\textcolor{green}{\Checkmark}}
\newcommand{\no}{\textcolor{red}{\XSolidBrush}}
\newcommand{\writing}{\textcolor{blue}{\Writinghand}}
\usepackage{bbding} % for checkmark and xsolid

\begin{document}
\begin{tikzpicture}[
  node distance = 1.5cm and 0.0cm,
  lwt/.style = {draw, rectangle, inner sep = 6pt, font = \large},
  refine/.style = {->, very thick}]

  \node[lwt, label = right : {\Large\no}] (abslwt) {\textcolor{red}{AbsLWT}};
  \node[lwt, below = of abslwt, label = right : {\Large\yes}] (caspaxos) {CASPaxos};
  \node[lwt, below left = of caspaxos, label = above : {\LARGE\writing}] (cpaxos) {Cassandra Paxos};
  \node[lwt, below right = of caspaxos, label = above : {\LARGE\writing}] (spaxos) {ScyllaDB Paxos};

  \draw[refine] (caspaxos) to (abslwt);
  \draw[refine] (cpaxos) to (caspaxos);
  \draw[refine] (spaxos) to (caspaxos);
\end{tikzpicture}
\end{document}