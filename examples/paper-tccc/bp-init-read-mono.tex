% bp-init-read-mono.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  co/.style = {->, thick},
  vo/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt, align = center}]

  % t': W(x, v')
  \node[txn, label = above : $t'$]
    (tprime) {$\writeevent(x, v'_x)$ \\[\vsep] $\writeevent(y, v'_y)$};

  % t: R(x, v)
  \node[txn, below = 1.00cm of tprime, label = below : $t$] (t)
    {$\readevent(y, v'_y) \; \readevent(x, \bot_x)$};

  % tprime-WRy-t
  \draw[wr] (tprime) to node[]{$\WR(y)$} (t);
\end{tikzpicture}
\end{document}