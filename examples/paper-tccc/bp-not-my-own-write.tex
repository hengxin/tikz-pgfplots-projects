% bp3-not-my-own-write.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, shapes.misc, positioning,
  arrows.meta, decorations.pathmorphing, fit}

\begin{document}
\begin{tikzpicture}

  \tikzset{
    so/.style = {->, thick},
    wr/.style = {->, thick},
    txn/.style = {rectangle, draw, inner sep = 2pt},
    txn crossed out/.style = {
      txn,
      append after command = {
        node[fit = (\tikzlastnode), draw = red, thick,
             inner sep=-\pgflinewidth, cross out]{}
      }
    }
  }

  \node[txn, label = left : $t$] (t) {$\cdots \writeevent(\keyxvar, \vvar) \cdots \readevent(\keyxvar, \vprimevar) \cdots$};
  \node[txn, above = 1.00cm of t, label = left : $t'$]
    (tprime) {$\cdots \writeevent(\keyxvar, \vprimevar) \cdots$};

  \draw[wr] (tprime.south) to node[]{$\WR(\keyxvar)$} (t.north);
\end{tikzpicture}
\end{document}