% bp-future-read-dots.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt}]

  \node[txn, label = left : $t$] (t) {$\cdots \readevent(x, v) \cdots \writeevent(x, v) \cdots$};
\end{tikzpicture}
\end{document}