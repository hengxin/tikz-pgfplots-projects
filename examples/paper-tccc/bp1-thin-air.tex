% bp1-thin-air.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt}]

  \node[txn, label = left : $t:$] (t) {$\cdots \readevent(\keyxvar, \vvar) \cdots$};
  \node[txn, above right = 1.20cm and -1.00cm of t, lightgray, label = left : \textcolor{lightgray}{$\nexists t':$}]
    (tprime) {$\cdots \writeevent(\keyxvar, \vvar) \cdots$};

  \draw[wr, lightgray] (tprime.south) to node[above = -0.10cm, sloped]{$\WR(\keyxvar)$} (t.north);
\end{tikzpicture}
\end{document}