% bp-thin-air.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt}]

  \node[txn, label = above : $t$] (t) {$\cdots \readevent(x, v) \cdots$};
  \node[txn, left = 1.20cm of t, lightgray, label = above : \textcolor{lightgray}{$t'$}]
    (tprime) {$\cdots \writeevent(x, v) \cdots$};

  \draw[wr, lightgray] (tprime) to node[above]{$\WR(x)$} (t);
\end{tikzpicture}
\end{document}