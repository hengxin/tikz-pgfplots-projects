% bp-future-read.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt}]

  \node[txn, label = above : $t$] (t)
    {$\readevent(x, v) \; \writeevent(x, v)$};
\end{tikzpicture}
\end{document}