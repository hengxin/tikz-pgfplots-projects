% bp4-intermediate-read.tex

\documentclass{standalone}
\input{newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, shapes.misc, positioning,
  arrows.meta, decorations.pathmorphing, fit}

\begin{document}
\begin{tikzpicture}

  \tikzset{
    so/.style = {->, thick},
    wr/.style = {->, thick},
    txn/.style = {rectangle, draw, inner sep = 2pt},
    txn crossed out/.style = {
      txn,
      append after command = {
        node[fit = (\tikzlastnode), draw = red, thick,
             inner sep=-\pgflinewidth, cross out]{}
      }
    }
  }

  \node[txn, label = left : $t$] (t) {$\readevent(x, v)$};
  \node[txn, above = 1.20cm of t, label = left : $t'$]
    (tprime) {$\writeevent(x, v) \quad \writeevent(x, v')$};

  \draw[wr] (tprime.-165) to node[above = -0.10cm, sloped]{$\WR(x)$} (t.north);
\end{tikzpicture}
\end{document}