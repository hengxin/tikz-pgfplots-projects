% bp-init-read-wr.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  co/.style = {->, thick},
  vo/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt, align = center}]

  % tprime: W(x, v')
  \node[txn, label = left : $t'$]
    (tprime) {$\writeevent(x, v'_x)$ \\[\vsep] $\writeevent(y, v'_y)$};
  % t: R(x, v)
  \node[txn, below = 1.00cm of tprime, label = left : $t$] (t)
    {$\readevent(x, \bot_x)$ \\[\vsep] $\readevent(y, v'_y)$};

  % tprime-WRy-t
  \draw[wr] (tprime) to node[]{$\WR(y)$} (t);
\end{tikzpicture}
\end{document}