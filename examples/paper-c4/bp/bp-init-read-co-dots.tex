% bp-init-read-co-dots.tex

\documentclass{standalone}
\input{../newcommands}

\usepackage{tikz}
\usetikzlibrary{shapes, positioning, arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}[
  so/.style = {->, thick},
  wr/.style = {->, thick},
  co/.style = {->, thick},
  txn/.style = {draw, inner sep = 2pt}]

  \node[txn, label = left : $t$] (t) {$\cdots \readevent(x, \bot_{x}) \cdots$};
  \node[txn, above = 1.00cm of t, label = left : {$t'$}]
    (tprime) {$\cdots \writeevent(x, v') \cdots$};

  \draw[co] (tprime) to node[left]{$\CO$} (t);
\end{tikzpicture}
\end{document}